import { createRouter, createWebHistory } from 'vue-router'

const routes = [] // Nenhuma rota por enquanto

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
